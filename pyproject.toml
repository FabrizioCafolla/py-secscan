[project]
name = "py-secscan"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pre-commit>=4.0.1",
    "ruff>=0.8.4",
]

[tool.uv.workspace]
members = ["py-secscan"]
